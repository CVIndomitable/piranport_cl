# 图标绘制规范（本次任务用）

你要为 Minecraft 模组《皮兰港》画一张 **32×32 物品贴图**。严格遵守以下规格。

## 画布与文件

- 尺寸 **32×32 像素**，保存为 **PNG**（RGBA）。
- 你只负责**画这一张图**并写到指定路径。**不要**创建/修改模型 JSON、不要碰 Java 代码。

## 硬性美术规格

1. **描边**：外轮廓一圈 **1px 纯黑 `(0,0,0,255)`**。
   - 只包外轮廓一圈，**不要 2px**，不要内侧再加暗色内环。
   - 纯黑**绝不**用于内部结构线或阴影。
2. **透明**：**二值 alpha**，只允许 0 和 255。边缘一刀切干净，绝大多数图标零半透明像素。
3. **配色**：手调小调色板，**平涂色块 + 极少量明暗块**，无渐变、无抖动、无噪点。
   - 本次 14 张均属「简单道具」档，**每张 ≤30 色**（不含透明）。
4. **明暗必须同色相阶梯**（比色数总数更重要）：
   - 暗部是**同一色相往下压**，不是换个颜色。
   - 参考（项目内辣椒贴图）：`#F47E82 → #ED3744 → #B71225` 一条红轴，不跳色。
   - 大色区内部连续过渡，色区之间边界干净。
5. **光**：统一**左上打光**（左上亮、右下暗）。

## 构图

- 装备/道具类取**斜角**：主体沿对角线从一角铺到另一角（左下→右上）。
- 主体尽量铺满画布，四边留 1–4px 空。
- 在 32×32 下辨识度优先：**先保证剪影能认出是什么**，再加细节。

## 可用工具

本机有 `python3`，如果 `PIL`（Pillow）可用，用脚本按像素写图是最可靠的方式：
```python
from PIL import Image
im = Image.new("RGBA", (32,32), (0,0,0,0))
# 逐像素 setim 或直接操作 px 数组
im.save("/path/to/out.png")
```
先跑 `python3 -c "import PIL; print(PIL.__version__)"` 确认。
若 PIL 不可用，用 `.venv_docx` 或 `.venv_ml` 虚拟环境里的 python，
或直接手写 PNG（不推荐）。**画完后必须用 PIL 重新打开校验**：
尺寸是 32×32、色彩数 ≤30、存在纯黑描边像素。

## 自检清单（提交前逐条确认）

- [ ] 文件是 32×32 RGBA PNG
- [ ] 外轮廓有 1px 纯黑 `(0,0,0,255)` 描边
- [ ] alpha 只有 0 和 255
- [ ] 不重复色数 ≤30
- [ ] 明暗是同色相阶梯，没有跳色
- [ ] 左上打光的明暗方向一致
