# 写给人类程序员

> 本项目由 Claude Code (AI) 协助开发。这份文档面向接手或参与本项目的真实人类，帮你快速理解代码结构、避免踩坑。

---

## 一、项目概述

| 项目 | 值 |
|------|----|
| Mod ID | `piranport` |
| 根包 | `com.piranport` |
| 平台 | NeoForge 21.1.220 / Minecraft 1.21.1 / Java 21 |
| 当前版本 | v0.0.10-alpha（Skin + Dungeon Script + 新武器/新敌人/燃料系统） |
| 构建工具 | Gradle + ModDevGradle |
| JAR 产物 | `build/libs/piranport-0.0.10.jar` |

---

## 二、整体目录结构

```
src/main/java/com/piranport/
├── PiranPort.java              ← Mod 入口，注册顺序在这里
├── ClientEvents.java           ← MOD总线客户端事件（Screen/Renderer/KeyMapping/ItemProperties 注册）
├── ClientGameEvents.java       ← GAME总线客户端事件（战场高亮 Glow）
├── CommonEvents.java           ← 通用事件（实体属性注册等）
├── GameEvents.java             ← 服务端游戏总线事件（水面行走、自动升空、飞机召回）
├── ClientTickHandler.java      ← 客户端每 tick（武器切换、火控按键、高亮、侦察控制、FC描边同步）
│
├── registry/                   ← 所有注册类，每类对象一个文件
│   ├── ModItems.java           ← 物品注册（100+ 个）
│   ├── ModBlocks.java          ← 方块注册（含 DUNGEON_LECTERN）
│   ├── ModBlockEntityTypes.java
│   ├── ModMenuTypes.java       ← 含 FlightGroupMenu, DUNGEON_BOOK_MENU
│   ├── ModRecipeTypes.java
│   ├── ModEntityTypes.java     ← 含 Aircraft/AerialBomb/Bullet/FloatingTarget/SanshikiPellet
│   │                             /LowTierDestroyer/DungeonTransportPlane/DungeonPortal/LootShip
│   ├── ModMobEffects.java      ← FloodingEffect/FlammableEffect/ReloadBoostEffect/EvasionEffect
│   ├── ModDataComponents.java  ← 含 AircraftInfo/FlightGroupData/FuelData/WeaponCooldown/DungeonProgress 等
│   ├── ModKeyMappings.java     ← V/P/O/I/U/Y/H/R/F8 九个按键
│   └── ModCreativeTabs.java
│
├── block/                      ← 方块逻辑
│   ├── StoneMillBlock.java
│   ├── CookingPotBlock.java
│   ├── CuttingBoardBlock.java  ← neighborChanged 检测发射器触发自动切割
│   ├── PlaceableFoodBlock.java ← 抽象类 + 3个静态内部类（Plate/Bowl/Cake）
│   ├── RiceCropBlock.java      ← 水稻（3阶段 waterlogged 作物）
│   ├── SaltChipBlock.java      ← 盐片薄层方块（蒸发产物）
│   ├── SaltEvaporationHandler.java ← 熔炉上方水→盐蒸发逻辑
│   ├── FourStageCropBlock.java
│   ├── ThreeStageCropBlock.java
│   └── entity/
│       ├── StoneMillBlockEntity.java   ← IItemHandler capability，AE2 兼容
│       ├── CookingPotBlockEntity.java  ← 顶/侧面输入、底面输出 capability
│       ├── CuttingBoardBlockEntity.java← IItemHandler 单槽
│       └── PlaceableFoodBlockEntity.java
│
├── item/
│   ├── ShipCoreItem.java       ← 最复杂的物品（火炮/鱼雷/飞机放飞、燃料补充）
│   │                             refillAircraftFuel() 现为 public static
│   │                             overrideOtherStackedOnMe() 实现收纳袋式装甲存取（无GUI模式）
│   │                             Shift+右键在无GUI模式已禁用（变身改为副手驱动）
│   ├── CannonItem.java         ← 含 R 键手动装填逻辑
│   ├── AircraftItem.java       ← 飞机编队物品 + 手动加油（overrideOtherStackedOnMe）
│   ├── TorpedoItem.java / TorpedoLauncherItem.java
│   ├── ArmorPlateItem.java
│   ├── SkinCoreItem.java       ← 皮肤核心（右键激活/切换皮肤）
│   ├── SonarItem.java          ← 声呐（有负重）
│   ├── UnicornHarpItem.java    ← 独角兽竖琴（16格范围 Regen I，10秒）
│   ├── ModFoodItem.java
│   ├── BottleFoodItem.java     ← 食用后返还玻璃瓶
│   ├── FloatingTargetItem.java ← 测试用靶子
│   └── GuidebookItem.java      ← Patchouli 软依赖教程书
│
├── menu/
│   ├── ShipCoreMenu.java / ShipCoreScreen.java ← 含舰装配置区域 + 自动升空按钮
│   ├── CookingPotMenu.java / CookingPotScreen.java
│   ├── StoneMillMenu.java / StoneMillScreen.java
│   └── FlightGroupMenu.java / FlightGroupScreen.java  ← 编组GUI（无容器槽模式）
│
├── entity/
│   ├── CannonProjectileEntity.java   ← 含友军伤害防护 + 击落归因
│   ├── TorpedoEntity.java            ← 含友军伤害防护 + 击落归因
│   ├── AircraftEntity.java           ← 状态机实体（6种状态）
│   │                                   含燃油消耗、空战、FOLLOW/RECON、isCurrentlyGlowing()
│   ├── AerialBombEntity.java         ← 航空炸弹（含友军伤害防护 + 击落归因）
│   ├── BulletEntity.java             ← 战斗机子弹（含友军伤害防护 + 击落归因）
│   ├── SanshikiPelletEntity.java     ← 三式弹子弹丸（8×8锥形散布，64发）
│   ├── AircraftDropEntity.java       ← 飞机击坠掉落物（防爆+浮水，类似下界之星）
│   ├── LowTierDestroyerEntity.java   ← 低阶驱逐舰NPC（副本教程敌人，水面轨道巡航+开炮）
│   ├── DungeonTransportPlaneEntity.java ← 副本运输机（空投脚本用）
│   └── FloatingTargetEntity.java
│
├── component/
│   ├── PlaceableInfo.java
│   ├── AircraftInfo.java       ← 8字段（手写StreamCodec，超过6字段限制）含 BombingMode
│   ├── FlightGroupData.java    ← 核心上的4组配置（slotAmmoTypes + attackMode）
│   │                              AttackMode：FOCUS / SPREAD / FOLLOW
│   ├── FuelData.java           ← 燃料数据（currentFuel / maxFuel）
│   ├── LoadedAmmo.java
│   ├── WeaponCategory.java     ← 武器种类标签枚举（AIRCRAFT/TORPEDO/CANNON/ARMOR）
│   ├── WeaponCooldown.java     ← endTick/totalTick/getFraction 冷却数据
│   └── SlotCooldowns.java      ← 分槽冷却 DataComponent（endTick+totalTick，存在核心上）
│
├── aviation/
│   ├── FireControlManager.java ← 服务端锁定目标列表（最多4个目标，含战斗机对地开关）
│   ├── ReconManager.java       ← 侦察机状态管理（operatorUUID → reconAircraftUUID）
│   ├── ClientFireControlData.java ← 客户端只读锁定列表（供HUD渲染）
│   └── ClientReconData.java    ← 客户端侦察状态（是否处于侦察视角）
│
├── client/
│   ├── ReloadBarDecorator.java     ← 物品装饰器，在核心图标上画装填进度条
│   ├── WeaponReloadDecorator.java  ← IItemDecorator 武器冷却条（橙=火炮/蓝=鱼雷）
│   ├── ShipCoreHudLayer.java       ← 旧版装填条（默认关闭，config 可开）
│   ├── FireControlHudLayer.java    ← 右上角锁定目标面板
│   ├── SkinOverlayLayer.java       ← 皮肤覆盖渲染层（64×64 PNG 叠加到玩家模型）
│   ├── GuiHelper.java              ← GUI 绘制工具类（槽位背景等）
│   ├── AircraftRenderer.java
│   ├── DungeonTransportPlaneRenderer.java
│   ├── LowTierDestroyerRenderer.java
│   ├── CuttingBoardRenderer.java
│   └── PlaceableFoodRenderer.java
│
├── effect/
│   ├── FloodingEffect.java     ← 进水（每秒1点魔法伤害）
│   ├── FlammableEffect.java    ← 易燃易爆（变身有燃料飞机时挂载）
│   ├── ReloadBoostEffect.java  ← 装填加速（I=2倍速 II=3倍速）
│   └── EvasionEffect.java      ← 高速规避（I=15% II=25% III=35%）
│
├── combat/
│   ├── TransformationManager.java ← 变身状态/武器槽/负重/移速/boostedCooldown()
│   │                                getCoreArmorBonus()/getCoreArmorLoad() 读取核心内护甲DataComponent
│   │                                isLoadItem() 已排除 ArmorPlateItem（护甲必须存入核心才计重）
│   ├── EvasionHandler.java        ← LivingIncomingDamageEvent 闪避判定
│   └── FriendlyFireHelper.java    ← 共用友军伤害防护工具类（所有抛射物共用）
│
├── skin/
│   ├── SkinManager.java        ← 服务端皮肤状态管理（playerUUID → skinId）
│   └── ClientSkinData.java     ← 客户端皮肤状态（供渲染层读取）
│
├── config/
│   ├── ModCommonConfig.java    ← 服务端配置（11项）
│   └── ModClientConfig.java    ← 客户端配置（2项）
│
├── compat/
│   └── jei/
│       ├── PiranPortJEIPlugin.java      ← JEI 插件入口
│       ├── CookingPotRecipeCategory.java
│       ├── StoneMillRecipeCategory.java
│       └── CuttingBoardRecipeCategory.java
│
├── dungeon/                    ← 副本系统（44个文件）
│   ├── DungeonConstants.java
│   ├── block/
│   │   └── DungeonLecternBlock.java     ← 副本入口讲台
│   ├── client/
│   │   ├── DungeonBookScreen.java       ← 副本选关界面
│   │   ├── DungeonResultScreen.java     ← 战斗结算
│   │   ├── DungeonReviveScreen.java     ← 复活界面
│   │   ├── TownScrollScreen.java        ← 回城卷轴确认
│   │   ├── DungeonHudLayer.java         ← 副本HUD
│   │   ├── DungeonPortalRenderer.java
│   │   └── LootShipRenderer.java
│   ├── data/
│   │   ├── DungeonDataLoader.java       ← JSON数据加载
│   │   ├── DungeonRegistry.java
│   │   ├── ChapterData.java / StageData.java / NodeData.java（含 script 字段）/ EnemySetData.java
│   ├── entity/
│   │   ├── DungeonPortalEntity.java     ← 副本传送门
│   │   └── LootShipEntity.java          ← 箱子船（战利品，dropping模式=垂直下落）
│   ├── event/
│   │   └── DungeonEventHandler.java     ← 死亡/登出/进度事件/脚本节点分发
│   ├── instance/
│   │   ├── DungeonInstance.java
│   │   ├── DungeonInstanceManager.java
│   │   └── NodeBattleField.java         ← 节点战斗场
│   ├── item/
│   │   └── TownScrollItem.java          ← 回城卷轴
│   ├── key/
│   │   ├── DungeonKeyItem.java          ← 副本钥匙
│   │   ├── DungeonProgress.java
│   │   └── FlagshipManager.java
│   ├── lobby/
│   │   └── DungeonLobbyManager.java     ← 大厅管理
│   ├── menu/
│   │   └── DungeonBookMenu.java
│   ├── network/                         ← 12个 C2S/S2C Payload
│   │   ├── ClientDungeonData.java
│   │   ├── DungeonStatePayload.java / DungeonResultPayload.java
│   │   ├── JoinLobbyPayload.java / LeaveLobbyPayload.java / LobbyUpdatePayload.java
│   │   ├── SelectStagePayload.java / SelectNodePayload.java / NodeEnteredPayload.java
│   │   ├── PlayerDiedInDungeonPayload.java / ReviveRequestPayload.java
│   │   └── TownScrollUsePayload.java
│   ├── script/                          ← 脚本化节点流程
│   │   ├── DungeonScript.java           ← 脚本接口
│   │   ├── DungeonScriptManager.java    ← 脚本管理器（ServerTick驱动）
│   │   └── ArtilleryIntroScript.java    ← 炮击引入3阶段脚本（空投→拾取→战斗）
│   └── saved/
│       ├── DungeonSavedData.java
│       └── DungeonLeaderboard.java
│
├── debug/
│   └── PiranPortDebug.java
│
├── network/                    ← 主网络包（16个）
│   ├── ModPackets.java              ← 所有 C2S/S2C 包注册
│   ├── CycleWeaponPayload.java
│   ├── FireControlPayload.java      ← P/O/I 火控 C2S
│   ├── FireControlSyncPayload.java  ← S2C 同步锁定列表到客户端
│   ├── OpenFlightGroupPayload.java
│   ├── FlightGroupUpdatePayload.java
│   ├── ReconControlPayload.java     ← 侦察机 WASD 控制 C2S
│   ├── ReconExitPayload.java        ← 退出侦察视角 C2S
│   ├── ReconStartPayload.java       ← 通知客户端切换摄像头 S2C
│   ├── ReconEndPayload.java         ← 通知客户端恢复摄像头 S2C
│   ├── AutoLaunchTogglePayload.java ← 切换战斗机自动升空开关 C2S
│   ├── ManualReloadPayload.java     ← R 键手动装填 C2S
│   ├── RecallAllAircraftPayload.java← 召回所有飞机 C2S
│   ├── DebugTogglePayload.java
│   ├── SkinSyncPayload.java / SkinRevertPayload.java ← 皮肤同步 S2C
│   └── SnapshotRequestPayload.java
│
├── recipe/
│   ├── CookingPotRecipe.java + CookingPotRecipeInput.java
│   ├── StoneMillRecipe.java + StoneMillRecipeInput.java
│   ├── CuttingBoardRecipe.java
│   └── ModBrewingRecipes.java
│
└── worldgen/
    ├── SaltGenBiomeModifier.java    ← 条件性盐矿生成（受 config 开关控制）
    └── ModBiomeModifiers.java       ← 在 registry/ 下
```

---

## 三、核心架构模式

### 3.1 无 GUI 模式变身机制（舰R模式）

变身状态由**副手槽**驱动（`tickInventoryLoadCheck` 每 tick 检测）：

- 副手放入 ShipCoreItem → 自动 `setTransformed(true)` + `applyAttributes` + `refillFuel` + 消息
- 副手取走 ShipCoreItem → 自动 `setTransformed(false)` + `removeAttributes` + 召回飞机 + 消息
- `ShipCoreItem.use()` 的 Shift+右键分支在无GUI模式直接 `return pass`（禁用）
- `KANSEN_MODE` 配置项（默认 true）强制启用无GUI模式，覆盖 `shipCoreGuiEnabled`

护甲板通过 `overrideOtherStackedOnMe` 以收纳袋方式存入核心（`SHIP_CORE_ARMOR` DataComponent），存入的护甲才计入负重和护甲加成；背包里散放的护甲板不再有效。

无GUI模式专属操作：
- **空手右键**：召回所有舰载机
- **H键**：切换战斗机自动升空
- **U键**：切换战斗机对地攻击模式（默认仅对空）
- **R键**：手动装填（大中口径火炮）

### 3.2 注册流程（必须理解）

DataComponents → Blocks → Items → CreativeTabs → MenuTypes → EntityTypes → MobEffects → BlockEntityTypes → RecipeTypes

KeyMapping 在 `ClientEvents.registerKeyMappings()` 注册（MOD 总线，客户端）。

### 3.3 功能方块标准四件套

每个带 GUI 的功能方块：`XxxBlock` + `XxxBlockEntity` + `XxxMenu` + `XxxScreen`，加上对应注册和 `ClientEvents` 绑定。

### 3.4 AircraftEntity 状态机（6种状态）

```
LAUNCHING(30tick) → CRUISING(绕轨) → ATTACKING → RETURNING → REMOVED
                        ↓
                  RECON_ACTIVE（侦察机专属）
```

- 继承 `Entity`，不继承 Mob，`noPhysics = true`，手动管理移动
- 攻击分派在 `tickAttacking()` 中按 `aircraftType` switch
- FIGHTER 特有 `resolveFighterTarget()` — 优先火控目标，其次敌方飞机，最后 Monster
- 攻击机（轰炸机+鱼雷机）不攻击空中目标，仅战斗机参与对空作战
- 四重防御：滞空上限 / 距离TP / 卡死检测 / 右键召回
- 返航物品通过 `coreInventorySlot` 精准写回，返回时 `currentFuel` 清零
- 每种飞机有血量（FIGHTER=20，RECON=10 等），被击落无物品返还
- 击坠掉落物使用 `AircraftDropEntity`（防爆+浮水，类似下界之星）

### 3.5 燃油系统

舰装核心有 `SHIP_CORE_FUEL`（`FuelData`）组件，存储 currentFuel/maxFuel。

- 支持燃料类型：航空燃料（FUEL）、煤炭、烈焰棒
- 飞机飞行中每 4 tick 消耗 1 燃油
- 燃油耗尽 → 直接返航（`isForcedReturn`）
- 侦察机无输入时沿当前朝向以最低速度前进（禁止悬停）

### 3.6 FlightGroupData 结构

存在**核心物品的 DataComponent** 上，不在飞机物品上：

```java
groups: [ { slotIndices: Set<Integer>, slotAmmoTypes: Map<Integer,String>,
            attackMode: FOCUS|SPREAD|FOLLOW, sortOrder: int } × 4 ]
```

弹种是**每架飞机独立配置**（`slotAmmoTypes`），不是整组共享。
FOLLOW 模式：巡航时跟随玩家的侦察机轨道飞行；有锁定目标时进入 ATTACKING。

### 3.7 火控系统数据流

```
客户端 P/O/I 按键
  → FireControlPayload(C2S)
  → 服务端 FireControlManager.lock/addTarget/clearTargets(ownerUUID, targetUUID)
  → FireControlSyncPayload(S2C)
  → 客户端 ClientFireControlData（只读，供 HUD 渲染）
  → AircraftEntity.resolveTarget() 直接读 FireControlManager（服务端）
```

火控描边：客户端记分板队伍 `pp_fc_target`（RED色），`ClientTickHandler.syncFcTeam()` 每tick同步。

自动升空时 `GameEvents.tickAutoLaunchFighters()` 在服务端直接调用 `FireControlManager.lock()` 锁定幻翼，无需经过 P 键流程。

战斗机默认**仅对空**（`isFighterAirOnly=true`），U 键切换对地攻击模式。

### 3.8 编组GUI无槽模式

`FlightGroupMenu` 继承 `AbstractContainerMenu`，但**0个容器槽**。玩家物品栏槽位设 x=-2000 隐藏。所有交互通过 C2S Payload（`FlightGroupUpdatePayload`）直接修改核心 DataComponent，不走 slot 机制。

### 3.9 加工站 IItemHandler Capability

厨锅、砧板、石磨均通过 `RegisterCapabilitiesEvent` 注册 `Capabilities.ItemHandler.BLOCK`：

- **厨锅**：`direction == DOWN` → 输出接口（只读输出槽）；其他方向 → 输入接口（仅武器槽）
- **砧板**：单槽，任意方向均可读写
- **石磨**：4输入2输出，任意方向均可（AE2 已兼容）

发射器联动：`CuttingBoardBlock.neighborChanged()` 检测相邻方块是否为 `DispenserBlock` 且 `TRIGGERED=true` 时自动执行一次切割。

### 3.10 侦察机系统

```
玩家 Shift+右键放飞侦察机
  → launchAircraft → AircraftEntity（RECON 类型）
  → LAUNCHING → RECON_ACTIVE（跳过 CRUISING）
  → ReconManager.startRecon(playerUUID, reconUUID)
  → ReconStartPayload(S2C) → 客户端 Minecraft.setCameraEntity(reconEntity)
  → 客户端每 tick: ReconControlPayload(dx,dy,dz)(C2S)
  → 服务端 ReconManager.handleControl() → AircraftEntity.tickReconActive()
  → 维护周围 3×3 区块强制加载
```

侦察机模式下禁用空手右键操作（防止皮肤还原和舰载机召回出错）。

### 3.11 战斗机自动升空

舰装配置开关存在舰装核心的 `SHIP_AUTO_LAUNCH`（Boolean DataComponent）上。GUI模式通过 ShipCoreScreen 按钮切换；无GUI模式通过 **H键** 切换。

```
每 40 tick（服务端 onPlayerTick）
  → 检查 SHIP_AUTO_LAUNCH == true
  → 检测 64 格内 Phantom
  → 若无活跃火控锁定：FireControlManager.lock(nearest phantom)
  → ShipCoreItem.tryAutoLaunchFighter()
      → refillAircraftFuel()（从弹药库取航空燃料补满飞机）
      → 找第一个有弹药配置的 FIGHTER 武器槽
      → launchAircraft()（消耗弹药、清空槽位、生成实体）
```

### 3.12 友军伤害防护

`FriendlyFireHelper.shouldBlockHit()` 被所有抛射物共用：
- `FRIENDLY_FIRE_ENABLED=false` 时跳过对其他玩家的伤害
- 始终跳过对自己飞机的伤害
- 抛射物记录 owner UUID，击落飞机时通过 owner 追溯到发射者

### 3.13 手动装填（R键）

大中口径火炮支持 R 键手动装填（`ManualReloadPayload` C2S）。弹药选择逻辑与自动装弹一致，从背包中消耗对应口径炮弹。

### 3.14 三式弹系统

三式弹（Sanshiki）为特殊炮弹类型，发射后在空中散布为 64 发 `SanshikiPelletEntity`（8×8锥形散布），每发子弹丸伤害为同口径 HE 弹的 1/4。适合对空射击。

### 3.15 皮肤系统

- `SkinCoreItem`：右键激活/切换皮肤（skinId 1~3）
- `SkinManager`（服务端）：维护 playerUUID → skinId 映射
- `ClientSkinData`：客户端皮肤状态
- `SkinOverlayLayer`：渲染层，将 `assets/piranport/textures/skin/skin_{id}.png`（64×64）叠加到玩家模型
- `SkinSyncPayload` / `SkinRevertPayload`：S2C 同步

### 3.16 副本系统

独立维度 `piranport:dungeon`，JSON驱动关卡数据，实例化战斗场，大厅组队机制。

- **数据层**：`data/piranport/dungeon/` 下 chapters/stages/enemy_sets JSON，`DungeonDataLoader` 加载
- **实例管理**：`DungeonInstanceManager` 管理多个 `DungeonInstance`，每个包含 `NodeBattleField`
- **脚本化节点**：`NodeData.script` 字段触发脚本分发；`DungeonScriptManager` 每tick驱动
- **运输机**：`DungeonTransportPlaneEntity` 直线飞行+空投+爬升消失
- **低阶驱逐舰**：`LowTierDestroyerEntity`（Monster），水面轨道巡航，30格侦测，100tick开炮
- **JEI兼容**：`PiranPortJEIPlugin` 注册3个加工站配方Category，`localRuntime` 软依赖

---

## 四、已踩坑清单（必读）

| 坑 | 错误写法 | 正确写法 |
|----|---------|---------|
| 酿造事件总线 | `modEventBus.addListener(RegisterBrewingRecipesEvent)` | `NeoForge.EVENT_BUS.addListener(...)` |
| 酿造旧API | `BrewingRecipeRegistry.addRecipe(recipe)` | `event.getBuilder().addRecipe(recipe)` |
| 食物饱和度 | `food.saturationModifier()` | `food.saturation()` |
| 熔炉热源检测 | `AbstractFurnaceBlockEntity.isLit()`（private）| `bs.getBlock() instanceof AbstractFurnaceBlock && bs.getValue(LIT)` |
| PlaceableFoodBlock codec | 单父类 `simpleCodec(Base::new)` | 三个静态内部类各自实现 `simpleCodec` |
| StreamCodec 超6字段 | `StreamCodec.composite(...)` | `StreamCodec.of(encoder, decoder)` 手写 |
| 编组GUI开启 | 客户端 Screen 直接 openMenu | 发 C2S 包 → 服务端 `serverPlayer.openMenu()` |
| 实体着火 | `entity.setSecondsOnFire(int)` | `entity.setRemainingFireTicks(int ticks)` |
| 从非LivingEntity发射抛射物 | 直接 new TorpedoEntity(level, shooter) | `new TorpedoEntity(type, level)` + 手动 `setPos/setDeltaMovement/setOwner` |
| 返回物品到背包 | `player.getMainHandItem()` | 存储 `coreInventorySlot`，返航时 `player.getInventory().getItem(slot)` |
| instanceof 模式变量 | `e instanceof Foo && e.getFoo()` | `e instanceof Foo f && f.getFoo()` |
| Inventory offhand槽位 | 无常量 | `player.getInventory().getItem(40)` |
| Phantom 不是 Monster | `instanceof Monster` 无法匹配 Phantom | `instanceof Phantom` 或走 FireControlManager 锁定路径 |
| 武器冷却共享 | `player.getCooldowns().addCooldown(this, ...)` | 用 `SLOT_COOLDOWNS` DataComponent 分槽记录 |
| 客户端实体描边 | `setGlowingTag(true)` 客户端无效 | 重写 `isCurrentlyGlowing()`，客户端读静态标志 |
| 彩色实体描边 | `entity.setGlowingTag` + team color 不同步 | 客户端记分板队伍 + `ChatFormatting.RED` 等颜色，每tick `syncFcTeam()` |
| 自定义维度注册 | 代码注册 dimension | `data/piranport/dimension/` + `dimension_type/` JSON 文件 |
| Patchouli 书籍结构 | `data/` 下 → `use_resource_pack` 异常 | `book.json` 加 `"use_resource_pack": true`；JSON 放 `assets/` |
| Patchouli 软依赖 | 直接Maven成硬依赖 | `localRuntime` + BlameJared maven + 反射调用 |
| 条件性BiomeModifier | 标准 `add_features` 无法动态开关 | 自定义 `BiomeModifier` + config 读取 |

---

## 五、配置系统

### 服务端配置（ModCommonConfig.java）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `saltGenerationEnabled` | `false` | 河流盐矿自然生成 |
| `shipCoreGuiEnabled` | `false` | 舰装核心GUI开关 |
| `autoResupplyEnabled` | `false` | 自动装填模式 |
| `fighterAmmoEnabled` | `false` | 战斗机子弹消耗 |
| `flammableEffectEnabled` | `false` | 易燃易爆Buff |
| `weaponPickupToInventory` | `false` | 武器拾取进背包 |
| `hotbarOnlyLoad` | `false` | 仅快捷栏负重（无GUI模式） |
| `friendlyFireEnabled` | `true` | 允许玩家间友军伤害 |
| `explosionBlockDamage` | `true` | 炮弹爆炸破坏方块 |
| `kansenMode` | `true` | 舰R模式（强制无GUI，覆盖 shipCoreGuiEnabled） |
| `slavPrisonMode` | `false` | 斯拉夫大牢模式 |
| `giveGuidebookOnFirstJoin` | `true` | 首次加入赠送教程书 |

### 客户端配置（ModClientConfig.java）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `showLegacyReloadHud` | `false` | 旧版装填HUD条 |
| `flightGroupEnabled` | `false` | 启用编组配置UI |

---

## 六、如何添加新内容

### 添加普通物品

1. `ModItems.java`：`ITEMS.registerSimpleItem("id")`
2. `models/item/id.json`：`item/generated` 父模型
3. `lang/*.json`：添加翻译（`zh_cn.json` + `en_us.json`）
4. `ModCreativeTabs.java`：加到创造模式标签

### 添加食物（带放置功能）

```java
ITEMS.register("my_food", () -> new ModFoodItem(new Item.Properties()
    .food(fp(5, 6.3f)
        .effect(new MobEffectInstance(MobEffects.MOVEMENT_SPEED, 200, 1), 1.0f)
        .build())
    .component(ModDataComponents.PLACEABLE_INFO.get(), new PlaceableInfo("plate", 2))));
```

### 添加厨锅/石磨/砧板配方

在 `data/piranport/recipe/` 新建 JSON，`type` 字段分别为：
- `piranport:cooking_pot`（含 `cookingTime` 字段）
- `piranport:stone_mill`
- `piranport:cutting_board`（含 `cuts` 字段）

### 添加酿造配方

在 `ModBrewingRecipes.java` 的 `register()` 方法中追加 `builder.addRecipe()`。

### 添加新的舰装配置开关

1. `ModDataComponents.java`：注册新的 Boolean DataComponent（参考 `SHIP_AUTO_LAUNCH`）
2. 新建对应 Payload（参考 `AutoLaunchTogglePayload`）
3. `ModPackets.java`：注册新 Payload
4. `ShipCoreMenu.java`：新增 `isXxx()` getter
5. `ShipCoreScreen.java`：在"舰装配置"区域增加新按钮
6. 在 `GameEvents.java` 或专门的服务端事件类实现服务端逻辑

### 添加新的副本关卡

1. `data/piranport/dungeon/chapters/` 下新建 chapter JSON
2. `data/piranport/dungeon/stages/` 下新建 stage JSON（引用 chapter）
3. `data/piranport/dungeon/enemy_sets/` 下新建 enemy_set JSON
4. 若需脚本化节点：实现 `DungeonScript` 接口，在 `DungeonScriptManager` 注册

---

## 七、事件总线区别

| 总线 | 用于 | 使用方式 |
|------|------|---------|
| `modEventBus`（构造函数传入）| 注册、FML Setup、DataGen | `modEventBus.addListener(...)` |
| `NeoForge.EVENT_BUS` | 游戏运行时事件（含酿造注册）| `NeoForge.EVENT_BUS.addListener(...)` |

---

## 八、开发环境搭建

```bash
git clone https://github.com/CVIndomitable/piranport_cl.git
cd piranport_cl
./gradlew genIntellijRuns   # 生成 IntelliJ 运行配置
./gradlew build             # → build/libs/piranport-0.0.10.jar
./gradlew runClient         # 启动开发客户端
```

自动打包 Stop Hook **已关闭**。需要时手动执行 `./gradlew build`，再复制 JAR 到 mods 目录。

---

## 九、数值速查

| 内容 | 数值 |
|------|------|
| 小/中/大型核心负重上限 | 40 / 64 / 112 |
| 小/中/大型火炮负重 | 6 / 16 / 30 |
| 双/三/四联鱼雷发射器负重 | 8 / 12 / 20 |
| 小/中/大装甲板负重/护甲 | 10/+2、20/+4、30/+6 |
| 战斗机/俯冲/鱼雷/水平轰炸机/侦察机负重 | 8 / 10 / 12 / 12 / — |
| 飞机最大滞空时间 | 12000 tick（10分钟） |
| 飞机最大距玩家距离 | 48格（FOLLOW/侦察机 200格） |
| 飞机血量（FIGHTER/DIVE/TORP/LEVEL/RECON） | 20 / 15 / 15 / 12 / 10 |
| 飞机燃油消耗 | 每 4 tick 消耗 1 燃油 |
| 火控锁定范围 | 80格 |
| 火控最大锁定目标数 | 4 |
| 战斗机弹药容量 | 64发 |
| 三式弹子弹丸数量 | 64发（8×8散布） |
| 自动升空检测范围 | 64格（幻翼） |
| 自动升空检测间隔 | 40 tick（2秒） |
| 进水 debuff 伤害 | 每秒 1 点魔法伤害 |
| 低阶驱逐舰侦测范围 | 30格 |
| 低阶驱逐舰开炮间隔 | 100 tick |
| 独角兽竖琴范围 | 16格（Regen I, 10秒） |
| 移速公式 | `max(0.4, 1.0 - totalLoad/maxLoad × 0.6)` |

---

## 十、按键绑定

| 按键 | 功能 | 说明 |
|------|------|------|
| V | 切换武器 | 循环下一个武器槽 / 退出侦察视角 |
| P | 火控锁定 | 替换锁定目标 |
| O | 火控追加 | 追加锁定目标（最多4个） |
| I | 火控取消 | 清除所有锁定 |
| U | 编组/对地 | GUI模式=打开编组UI；无GUI模式=切换战斗机对地攻击 |
| Y | 高亮 | 切换战场实体高亮显示 |
| H | 自动升空 | 切换战斗机自动升空开关 |
| R | 手动装填 | 大中口径火炮手动装填 |
| F8 | 调试 | 切换调试模式 |

---

## 十一、版本规划速览

| 版本 | 代号 | 状态 | 核心新增 |
|------|------|------|---------|
| v0.0.1 | MVP | ✅ | 矿物、舰装核心GUI、火炮 |
| v0.0.2 | Torpedo | ✅ | 鱼雷、进水debuff、装填HUD |
| v0.0.3 | Kitchen | ✅ | 食物烹饪、作物、加工站 |
| v0.0.4 | Aviation | ✅ | 飞机编队、编组GUI、火控、战场高亮 |
| v0.0.5 | Combat+ | ✅ | 装填Decorator、战斗Buff（装填加速/高速规避）、Buff食物 |
| v0.0.6 | Skin | ⏳ | 皮肤/模型渲染 |
| v0.0.7 | Aviation+ | ✅ | 侦察机视角、空战、FOLLOW编队、加工站自动化、战斗机自动升空 |
| v0.0.8 | Salt & Rice | ✅ | 稻田种植、盐蒸发系统、条件性盐矿生成、全面代码审查修复(35项) |
| v0.0.9 | Dungeon | ✅ | 副本系统、JEI兼容、武器冷却条、友军伤害防护、彩色描边、击落归因 |
| v0.0.10 | — | ✅ | 皮肤系统、副本脚本、三式弹/声呐/新飞机、低阶驱逐舰、燃料组件、手动装填 |
