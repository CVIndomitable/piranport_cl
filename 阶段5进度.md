# 阶段5进度报告

## 已完成
1. ✅ ModEntityTypes 注册类迁移完成（13个实体类型）
2. ✅ 批量复制并适配了所有entity类的基础API
3. ✅ 复制了combat、aviation、component、debug等依赖包
4. ✅ 修复了大部分1.20.1 API差异：
   - Component.Serializer API（移除registryAccess参数）
   - ResourceLocation API（fromNamespaceAndPath → new ResourceLocation）
   - ItemStack save/parse API
   - defineSynchedData方法签名
   - AttributeModifier.Operation枚举
   - OnlyIn注解包名

## 当前问题
由于阶段5聚焦Entity+Network，但发现entity类有大量依赖：
- **AircraftEntity**: 依赖ModDataComponents、network包、ItemContainerContents等（阶段7才处理）
- **CannonProjectileEntity/MissileEntity**: AttributeModifier API需要从ResourceLocation改为UUID
- **ForcedChunkData**: SavedData API签名差异
- **多个entity**: isCurrentlyGlowing方法在1.20.1不存在，需要删除

## 建议策略
阶段5应该：
1. 暂时注释掉AircraftEntity中依赖ModDataComponents的代码
2. 完成简单entity类的编译（TorpedoEntity、BulletEntity等）
3. **重点完成Network包迁移**（SimpleChannel重写）
4. 复杂entity的完整功能留到阶段7（NBT重写）时一起处理

## 编译状态
当前编译错误约78个，主要集中在：
- AircraftEntity（依赖未迁移的DataComponents）
- AttributeModifier API差异
- SavedData API差异
- isCurrentlyGlowing方法删除不完整
